# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Project

**SST-test** is part of the Calypso Monterey Bay (CMB) project. It organizes and
enables tests to collect Sea Surface Temperature (SST) data for Monterey Bay.

## Git

- **The user (J. Xavier Prochaska) will perform all git commands** — commits,
  branches, merges, pushes, pulls, etc.
- Do **not** run `git commit`, `git push`, or any state-changing git command
  unless explicitly asked.
- Read-only git inspection (`git status`, `git log`, `git diff`) is fine when it
  helps with a task.

## Conventions

- This is a Python project (see `claude_prompts/start_up.md` for setup intent).
- Record completed work in the **Logs** section of
  `claude_prompts/start_up.md` using the documented date + summary format.
